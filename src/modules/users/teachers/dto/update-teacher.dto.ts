// Teacher DTO for updates - Teachers only have userId which cannot be changed
// If needed, teacher updates would be handled through the User entity
export class UpdateTeacherDto {
  // No updateable fields for Teacher entity
  // Teacher updates should be done through the associated User entity
}
